#![forbid(unsafe_code)]

/// Core domain language, invariants, and lifecycle types for the
/// Cross-Chain Transfer Reconciliation Gateway.
///
/// Milestone 0 intentionally keeps this crate minimal.
/// It exists so the workspace builds and the architecture is explicit.
pub const CRATE_NAME: &str = "domain";
